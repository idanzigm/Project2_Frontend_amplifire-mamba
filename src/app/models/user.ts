export class User {
    //this class is the same as the user class in the backend
    //TODO: will need to fill out constructor more after fleshing out backend user class
    constructor(public userId:number, public username:string, public password:string, public email:string, public firstName:string, public lastName:string){}

}
