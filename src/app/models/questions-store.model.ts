export class QuestionsStore {

    store:Array<object>; 

    constructor(store:Array<object>) {
        this.store = store; 
    }

    appendStore(question:object) {
        this.appendStore(question);
    }
}
