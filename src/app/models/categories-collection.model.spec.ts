import { CategoriesCollection } from './categories-collection.model';

describe('CategoriesCollection', () => {
  it('should create an instance', () => {
    expect(new CategoriesCollection()).toBeTruthy();
  });
});
